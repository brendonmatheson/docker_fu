version: "3"
services:

    api1:
        build: .

    api2:
        build: .

    api3:
        build: .

    haproxy:
        image: library/haproxy:1.7
        volumes:
          - haproxy_cfg:/usr/local/etc/haproxy
        ports:
            - "9871:80"
        expose:
            - "9871"
        links:
            - api1
            - api2
            - api3

volumes:
  haproxy_cfg:
    external: true
