version: "3"
services:

    api1:
        build: .
        image: myco/tasks

    api2:
        build: .
        image: myco/tasks

    api3:
        build: .
        image: myco/tasks

    haproxy:
        image: library/haproxy:1.7
        volumes:
          - haproxy_cfg:/usr/local/etc/haproxy
        ports:
            - "9880:80"
            - "9881:70"
        links:
            - api1
            - api2
            - api3

volumes:
  haproxy_cfg:
    external: true
